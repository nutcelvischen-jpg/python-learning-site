---
title: "5.2 讀寫資料"
---

<div class="lesson-header">
  <span class="chapter-tag">第 5 章 · pandas</span>
  <h1>5.2 讀寫資料</h1>
  <div class="meta">
    <span>⏱️ 30 分鐘</span>
    <span>📖 互動式</span>
    <span>🎯 資料分析</span>
  </div>
</div>

## 🎯 學習目標

- 讀寫 CSV / Excel / JSON
- 設定 index / parse_dates
- 處理編碼問題


## 📥 讀檔

```python
import pandas as pd

# CSV
df = pd.read_csv("data.csv")
df = pd.read_csv("data.csv", encoding="utf-8")
df = pd.read_csv("data.csv", index_col="id")
df = pd.read_csv("data.csv", parse_dates=["date"])

# Excel（要裝 openpyxl）
df = pd.read_excel("data.xlsx", sheet_name="Sheet1")

# JSON
df = pd.read_json("data.json")

# 從 SQL
# df = pd.read_sql("SELECT * FROM users", conn)
```


## 📤 寫檔

```python
df.to_csv("out.csv", index=False, encoding="utf-8")
df.to_excel("out.xlsx", index=False, sheet_name="結果")
df.to_json("out.json", orient="records", force_ascii=False)
```


## ❗ 常見錯誤

1. **編碼錯**：中文檔要 `encoding="utf-8"` 或 `big5`
2. **index 被寫進去**：用 `index=False`
3. **Excel 引擎**：要裝 `openpyxl`


## ✏️ 練習題

1. 讀 `taiwan_aqi_latest.csv` 進 pandas
2. 過濾出 AQI > 50 的測站，存成新的 CSV


